<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.client.loanapproval">

    <application
        android:name=".MainApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.registerlogin.RegisterLoginActivity" />
        <activity android:name=".ui.introslider.IntroSliderActivity" />
        <activity android:name=".ui.loan.GetLoanActivity"
            android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar"/>
        <activity android:name=".ui.kyc.PerformKYCActivity" />
        <activity android:name=".ui.enach.ENachActivity" />
        <activity android:name=".ui.agreement.AgreementActivity" />
    </application>

</manifest>